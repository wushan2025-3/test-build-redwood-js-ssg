{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "build": "npx rw build web --prerender false && node scripts/prerender.cjs"
  },
  "devDependencies": {
    "@redwoodjs/core": "8.9.0",
    "@redwoodjs/project-config": "8.9.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "prisma": {
    "seed": "npx rw exec seed"
  }
}
